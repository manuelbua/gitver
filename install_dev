#!/bin/bash

sudo rm -rf build dist &> /dev/null

sudo find . -name \*.pyc -delete
# build NEXT version info
bin/gitver update dev
sudo python2 setup.py develop
sudo rm -rf build dist &> /dev/null
sudo find . -name \*.pyc -delete